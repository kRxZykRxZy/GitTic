# Cluster Configuration
# Copy this to .env and update with your values

# Backend platform URL
PLATFORM_URL=http://localhost:3000

# Cluster node configuration
CLUSTER_TOKEN=your-secure-cluster-token-here
CLUSTER_PORT=4000

# Publicly reachable URL for this node (recommended)
# Used during registration so the platform can schedule work to the correct address.
# Example: http://10.0.0.12:4000 or https://cluster-1.example.com
CLUSTER_PUBLIC_URL=http://localhost:4000

# Alternative to CLUSTER_PUBLIC_URL (if you want the agent to compose it)
# CLUSTER_PROTOCOL=http
# CLUSTER_HOST=your-hostname-or-ip

# Unique authentication token for this cluster (required)
# Generate a secure token: $(uuidgen) or any unique string

# (Optional) Custom node ID - defaults to UUID
# NODE_ID=my-cluster-1

# (Optional) Custom node name - defaults to hostname
# NODE_NAME=cluster-main

# (Optional) Comma-separated list of capabilities
# CAPABILITIES=docker,gpu,arm64

# (Optional) Maximum concurrent jobs this cluster can handle
# MAX_JOBS=10

# (Optional) Heartbeat interval in milliseconds (default: 15000)
# HEARTBEAT_MS=15000
