{
  "name": "@platform/ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@platform/utils": "workspace:*",
    "@platform/shared": "workspace:*"
  }
}
